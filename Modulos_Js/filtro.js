export const functionFiltro = (datos) => {
  const storage = JSON.parse(localStorage.getItem('posts'))
  console.log('🚀 ~ functionFiltro ~ storage:', storage)

  //   const filtroBusqueda = document.createElement('select')
  //   filtroBusqueda.placeholder = 'Filtro'
  //   const optionBusqueda = document.createElement('option')
  //   filtroBusqueda.append(optionBusqueda)
  //   optionBusqueda.textContent = storage.fecha
  //   container.prepend(filtroBusqueda)
  //   if (datos) {
  //   }
}
